---
name: project-crm-networker
description: CRM Networker — multi-tenant CRM+BI for network marketing; stack, modello, ADR firmati, stato build
metadata:
  type: project
---

**CRM Networker** = piattaforma CRM + Business Intelligence multi-tenant, enterprise, per organizzazioni di network marketing. Path: `e:\POWER AGENCY\SAAS\CRM\CRM Networker`. Repo GitHub: **CRM-Networker** (remote da collegare/push). UI italiana, i18n-ready.

**Stack (locked):** Supabase (Postgres 15 + Auth + RLS + Edge Functions + Realtime + pg_cron) backend; Next.js 14 App Router + TypeScript + Tailwind + shadcn/ui + Recharts frontend.

**Modello core (obbligatorio):** profilo **marketer** ≠ **account** utente. `marketers` esiste senza login (pre-registrazione); `memberships` collega `auth.users` a un profilo ESISTENTE (l'attivazione non ricrea mai il profilo). Multi-tenancy = `org_id` + `FORCE ROW LEVEL SECURITY` su ogni tabella, isolata via claim JWT `org_id` (`current_org_id()`).

**Genealogia = ALBERO BINARIO VERO.** `marketers.parent_id` + `leg` (un figlio LEFT + un RIGHT, partial-unique); `sponsor_id` separato per il credito di recruiting. Closure table (`marketer_tree_closure` + `branch_leg`) + path `ltree` = primitiva unica sia per la visibilità RLS (`can_see_marketer()`) sia per le analytics Left/Right. Placement **operator-driven** (no spillover) → `place_marketer(parent,leg,sponsor,...)`.

**Rank:** executive, consultant, team_leader, senior_team_leader, executive_team_leader, vice_president. CRM-eligible = consultant→vice_president. Attivazione account gated a **rank ≥ team_leader** (sottoalbero) OPPURE admin/owner.

**ADR firmati (2026-05-30)** — `docs/architecture/16-decision-log.md` (AUTORITATIVO, supera 01–15): ADR-001 placement operator-driven; ADR-002 compensi/volumi out-of-scope v1; ADR-003 attivazione rank-gated, flag `can_invite` rimossa (4 flag: crm_access, export_enabled, manage_documents, view_branch_comparison); ADR-004 MFA opzionale/phased; ADR-005…008 fix consistenza (RLS in doc 04/10; coda `app_private.dirty_metric_days`; hook JWT doc 10 §2.2; route map ADR-008).

**STATO BUILD:**
- ✅ **Architettura**: `docs/architecture/00-README.md` … `16-decision-log.md` (17 doc).
- ✅ **Backend Supabase**: `supabase/migrations/0001..0020` (tenancy, albero+closure/ltree triggers, auth/visibility, RLS su 24/24 tabelle, lifecycle, contatti, centos, sette-perché, documenti+versioning, prospects+journey, calls, notifiche, audit, fact layer+MV, leaderboard/bottleneck, reporting, cron) + `seed.sql` + `config.toml` + `BUILD-REPORT.md` (verdetto READY). Fix post-review applicati: O-1 (path in BEFORE INSERT), O-2 (`current_role`→`current_app_role`), O-3 (search_path ltree).
- ✅ **Frontend Slice 1** (`web/`, commit 3107c36): foundation condivisa (design system dark/light next-themes, primitive UI shadcn-style, `lib/types/db.ts`, data-layer Supabase server-only con **fallback mock**, scope provider Global/Sinistra/Destra, nav ADR-008); **auth** (/accedi, /recupera-password, /reimposta-password, /invito/[token]); **app shell** (sidebar gated, topbar con scope/tema/menu, mobile drawer, /dashboard rank-adaptive); **genealogia** binaria React Flow + d3-hierarchy (node card, pannello dettaglio, search, tab Sinistra/Destra). **`next build` verde** (verificato, exit 0, gira senza env in modalità demo). Avvio: `npm --prefix web run dev`.
- ✅ **Frontend Slice 2 (CRM)** (commit 4821815): /contatti (tabella+filtri+bulk), /percorso-prospect (+/[id], kanban 6 stadi dnd-kit + timeline storico), /sette-perche (+/[id], stepper guidato 7 perché), /centos, /documenti (editor Tiptap + versioning/duplica/archivia), /chiamate (registrazione+log+stats). `next build` verde (16 route, demo senza env).
- ✅ **Frontend Slice 3 (Analytics)** (lavorato 2026-05-31, non ancora committato): **/analytics** (scope switcher URL-driven Global/Sinistra/Destra, banda 6 KPI, trend 30gg sparkline SVG, funnel occupazione, conversione per fase, confronto rami, colli di bottiglia), **/classifiche** (controlli metrica/ambito/ramo client URL-driven, podio + tabella, `is_self` evidenziato), **/report** (tab Report/Esportazioni, card report con delta MoM, pulsanti export PDF/XLSX/CSV via server action, tabella job export), **/notifiche** (inbox client: filtro tutte/non lette, segna letta/tutte, rimuovi, deep-link per type+payload). Tipi+label aggiunti a `lib/types/db.ts`; data-layer `lib/data/{analytics,leaderboards,notifications,reports}.ts` + mock in `lib/data/mock/` (best-effort RPC reali `subtree_metrics`/`branch_metrics`/`funnel_totals_subtree`/`stage_conversion_subtree` + tabelle `bottleneck_findings`/`notifications`/`monthly_reports`/`report_export_jobs`, fallback mock). i18n `messages/it.json` esteso. **NB: nessuna lib chart installata → grafici in CSS/SVG puro (no Recharts).** `next build` verde (20 route, exit 0, demo senza env). Enqueue export reale via Edge = follow-up (ora simulato).
- ✅ **Frontend Slice 4 (Admin)** (lavorato 2026-05-31, non ancora committato): **/admin** (direzione CEO: 5 KPI org, azioni rapide, colli di bottiglia), **/admin/marketer** (registro: ricerca/filtri URL-driven, tabella profilo+account+ruolo+team) + **/admin/marketer/nuovo** (pre-registrazione via `place_marketer`, picker genitore/gamba/sponsor), **/admin/attivazioni** (inviti CRM: manager client crea/revoca, ADR-003), **/admin/ranghi** (distribuzione gradi + storico `rank_history`), **/admin/audit** (timeline `audit_log` con filtro azione), **/admin/impostazioni-org** (form org + soglie bottleneck), **/impostazioni** (profilo read-only + tema). Tipi+label in `lib/types/db.ts`; data-layer `lib/data/admin.ts` + `lib/data/admin-invitations.ts` + mock `lib/data/mock/admin.ts` (RPC reali `place_marketer`/`revoke_invitation` + tabelle `marketers`/`memberships`/`account_invitations`/`rank_history`/`audit_log`/`organizations`, fallback mock). `next build` verde (27 route, exit 0, demo senza env).
- ✅ **Edge Functions (Deno)** (lavorato 2026-05-31, non ancora committato): `supabase/functions/` con **create-invitation** (mint+hash token, RPC `create_invitation` sotto JWT del chiamante, link invito + email opzionale Resend), **activate-account** (anon: crea login `auth.users` via admin API + `accept_invitation(token_hash,user_id)`, profile-preserving, idempotente), **generate-report-export** (sync: `assemble_report_dataset` sotto RLS del chiamante → render CSV/JSON; PDF/XLSX = follow-up). `_shared/` (cors/http/token-sha256/supabase user+admin client), `deno.json`, README, registrate in `config.toml [functions.*]` (verify_jwt: create/export=true, activate=false). **Nuova migration `0021_invitation_context.sql`** (RPC SECURITY DEFINER anon-safe per la landing /invito, keyed su token_hash, solo pending+non scaduto). FE cablato al percorso reale con fallback demo: `admin-invitations.createInvitation`→invoke create-invitation; `invito/[token]/invite-form`→invoke activate-account (sostituito il vecchio rpc accept_invitation errato); `lib/data/invitation.ts`→rpc `invitation_context` (hash con node:crypto, non più la tabella inesistente `invitations`). `next build` verde. **NON testate a runtime** (no Deno/Supabase locale): corrette per contratto, da `supabase functions deploy` + verificare su progetto reale.
- ⏳ **Validazione DB**: niente Docker/psql locale → migrazioni (0001–0021) verificate solo staticamente; vanno applicate con `npx supabase db push` su un progetto Supabase reale (poi iterare sugli errori).
- ✅ **Git/GitHub**: repo su **github.com/poweragency/CRM-Networker** (`main`, pushato). Commit chiave: 89f9ea1 (arch+backend), 3107c36 (slice1 FE shell/auth/genealogia), 4821815 (slice2 CRM).

**COSA MANCA (prossimi step):** Frontend a route = FATTO (27 route: Slice 1–4). Edge Functions core (create-invitation/activate-account/generate-report-export) = FATTE ma da deployare+testare su progetto reale. Restano: **provisioning Supabase reale + `npx supabase db push`** delle migration 0001–0021 + `functions deploy` + abilitare l'access-token hook + creare il bucket `reports` (poi iterare sugli errori) — **questo è ora il blocco #1**; **Edge Functions residue** (pg_net cron fan-out per notifiche/report, branch-analytics/bottleneck-engine se servono fuori da pg_cron) + render binario PDF/XLSX in generate-report-export + path async drain `report_export_jobs`; **Realtime** per /notifiche (ora solo load iniziale SSR); **generazione tipi TS dal DB** (`supabase gen types`) a sostituzione degli hand-written in `lib/types/db.ts`; test (unit/e2e); hardening (MFA, rate limit, audit coverage). Affinamenti UI possibili: drag-to-move admin nella genealogia, dettaglio marketer in /admin/marketer, azioni cambio-grado/ruolo/stato, pulsante download export sync (invoke generate-report-export).

**Convenzione memoria:** questa è la memoria canonica in-repo. Lo stub a `~/.claude/projects/<slug>/memory/` rimanda qui. Al prossimo avvio: `git pull --rebase` prima di leggere. Vedi global CLAUDE.md.
